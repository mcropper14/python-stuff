# find velocity 

    velocity = (pos - last_values[0]) / (time - last_values[1])
    if abs(velocity - last_velocity) < (delta:)
        terminal_velocity = velocity
        break
     first_velocity = velocity
     last_values = [pos, time]
     last_values = velocity 
     
     first_values = [pos,time]
print("Velocity")   
def add(x + y)
return x + y 
